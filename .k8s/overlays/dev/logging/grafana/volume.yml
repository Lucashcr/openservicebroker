apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-volume
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /volumes/grafana
    type: DirectoryOrCreate
